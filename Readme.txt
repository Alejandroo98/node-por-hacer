Lo que hacemos aqui desde la console es poder crear una lista de tareas por hacer luego podemos actualizarlas y tamebien eliminarlas:

code app crear -d "Nueva tarea"
code app actualizar -d "Nueva tarea"  -c  true
code app borrar -d "Nueva tarea"